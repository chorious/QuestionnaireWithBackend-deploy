version: "3.8"

services:
  app:
    build: .
    container_name: questionnaire
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - ADMIN_TOKEN=${ADMIN_TOKEN}
      - DB_PATH=./data/data.db
    volumes:
      - ./data:/app/data
